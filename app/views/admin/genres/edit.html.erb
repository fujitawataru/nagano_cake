<h6>ジャンル編集</h6>
<%= form_with model:@genre,url:admin_genre_path(@genre.id), local:true do |f| %>
<p><strong>ジャンル名</strong> <%= f.text_field :name %> <%= f.submit "変更を保存",class:"btn btn-success" %></p>
<% end %>