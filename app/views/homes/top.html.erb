<div class="container">
  <div class= "row">
    <div class= "col-3">
       <table class="table">
      <thead class= "thead-light">
        <tr>
          <th>ジャンル検索</th>
        </tr>
      </thead>
      <% @genres.each do |genre| %>

      <tbody>
      <tr>
        <td><%= genre.name %></td>
      </tr>
      <% end %>
      </tbody>
    </table>
  </div>

    <div class="col-9">
      <div class="row">
       <div class="top_page-visual">
        <p class="top_page_introduction">
          ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員ではない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。</p>
       </div>
      </div>

      <div class="row">
        <h5><strong>新着商品</strong></h5>
       <% @items.each do |item| %>
        <%= link_to item_path(item.id) do %>
         <div><%= attachment_image_tag item, :image, :fill, 100, 100, format: 'jpeg' %></div>
         <div><%= item.name %></div>
         <div>¥<%= item.price.to_s(:delimited, delimiter: ',') %></div>
        <% end %>
        <% end %>
      </div>
    </div>
</div>